{
  "name": "requirejs-dependencies-performance",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "lint": "../node_modules/.bin/eslint --fix .",
    "test": "node generate-graph"
  },
  "devDependencies": {
    "benchmark": "2.1.4"
  }
}
